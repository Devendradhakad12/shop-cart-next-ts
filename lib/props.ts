export interface userProps {

    iat: number;
    id: string;
    name: string;
    role: string;
}

// token props 

export interface DataStoredInToken {
    id:string,
    role:string,
    name:string,

}